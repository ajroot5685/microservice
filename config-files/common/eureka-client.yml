# target service
# - apigateway-service
# - user-service
# - order-service
# - catalog-service
eureka:
  client:
    register-with-eureka: true
    fetch-registry: true
    service-url:
      defaultZone: http://localhost:8761/eureka